# `latch-scheme-httpsonly-config`

Configuration for a latch-scheme HTTP latch that implicitly denies all request schemes except for HTTPS.

## The `latch-scheme-httpsonly-config` World

- exports `wasi:config/store@0.2`
