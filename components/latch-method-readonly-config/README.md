# `latch-method-readonly-config`

Configuration for a latch-method HTTP latch that implicitly denies all request methods except for GET, HEAD, QUERY, and OPTIONS.

## The `latch-method-readonly-config` World

- exports `wasi:config/store@0.2`
