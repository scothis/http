# `latch-n5`

HTTP latch that aggregates five other HTTP latches.

## The `latch-n5` World

- imports `componentized:http/latch` (as latch, latch1, latch2, latch3, and latch4)
- exports `componentized:http/latch`
